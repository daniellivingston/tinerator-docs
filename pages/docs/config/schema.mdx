import withDocs from '../../../components/with_docs';

export default withDocs({
  title: 'Config Schema',
  description: 'The statickit.json schema reference',
  path: '/docs/config/schema',
  breadcrumb: {
    path: '/docs/config',
    label: 'Config',
    rel: 'prev'
  },
  toc: [
    [1, 'forms', '#forms']
  ],
  prev: {
    path: '/docs/config',
    label: 'Config'
  },
  next: {
    path: '/docs/html',
    label: 'HTML Library'
  }
});

# Schema

This document describes the `statickit.json` schema. Here are the available top-level keys:

| Key                     | Type                     | Description                                                  |
| :---------------------- | :----------------------- | :----------------------------------------------------------- |
| [`forms`](#forms)       | Object                   | An object defining the forms for your site                   |

## `forms`

An Object containing form configurations.

| Key                           | Type                     | Description                                                  |
| :---------------------------- | :----------------------- | :----------------------------------------------------------- |
| `name`                        | String (required)        | The name of the form                                         |
| [`actions`](#formskeyactions) | Object                   | Actions to perform after submission                          |
| [`fields`](#formskeyfields)   | Object                   | Optional metadata about form fields                          |

#### Usage Example

```js
{
  "forms": {
    "contact": {
      "name": "Contact Form"
    }
  }
}
```

## `forms[key].actions`

An Object containing actions to perform after the form is submitted.

| Key                                  | Type                   | Description                                                     |
| :----------------------------------- | :--------------------- | :-------------------------------------------------------------- |
| [`email`](#formskeyactionsemail)     | Array of Objects       | Sends an email notification                                     |
| [`webhook`](#formskeyactionswebhook) | Array of Objects       | Sends a webhook                                                 |

#### Usage Example

```diff
  {
    "forms": {
      "contact": {
        "name": "Contact Form",
+       "actions": {
+         "email": [{
+           "to": "john@example.com"
+         }],
+         "webhook": [{
+           "url": "http://example.com/hook"
+         }]
+       }
      }
    }
  }
```

## `forms[key].actions.email`

An Array of Objects containing email notification actions.

| Key                        | Type                     | Description                                                  |
| :------------------------- | :----------------------- | :----------------------------------------------------------- |
| `to`                       | String (required)        | The email address of the recipient                           |

#### Usage Example

```diff
  {
    "forms": {
      "contact": {
        "name": "Contact Form"
+       "actions": {
+         "email": [{
+           "to": "john@example.com"
+         }]
+       }
      }
    }
  }
```

## `forms[key].actions.webhook`

An Array of Objects containing webhook actions.

| Key                        | Type                     | Description                                                  |
| :------------------------- | :----------------------- | :----------------------------------------------------------- |
| `url`                      | String (required)        | The endpoint to send the POST request                        |

#### Usage Example

```diff
  {
    "forms": {
      "contact": {
        "name": "Contact Form"
+       "actions": {
+         "webhook": [{
+           "url": "http://example.com/hook"
+         }]
+       }
      }
    }
  }
```

## `forms[key].fields`

An Object containing optional field metadata. 

**Note:** By default, StaticKit will accept any fields you pass in from the client-side. However, defining your fields here allows you to add validations (such as marking a field as required) and give them human-friendly names.

Field objects can contain the following properties:

| Key                                  | Type                   | Description                                                     |
| :----------------------------------- | :--------------------- | :-------------------------------------------------------------- |
| `type`                               | String                 | The field type (`text` or `email`)                              |
| `prettyName`                         | String                 | The human-friendly name of the field                            |
| `required`                           | Boolean                | Specifies whether the field is required                         |

#### Usage Example

```diff
  {
    "forms": {
      "contact": {
        "name": "Contact Form",
+       "fields": {
+         "email": {
+           "type": "email",
+           "prettyName": "Email address",
+           "required": true
+         }
+       }
      }
    }
  }
```
