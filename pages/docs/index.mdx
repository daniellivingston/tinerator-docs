import withDocs from '../../components/with_docs';

export default withDocs({
  title: 'Documentation',
  description: 'How to use StaticKit client libraries and APIs.',
  path: '/docs'
});

# Introduction

StaticKit is the modern way to gather form submissions from your website. We have out-of-the-box support for modern React-based site generators (like Next.js and Gatsby) and traditional HTML environments (like Jekyll and Hugo).
