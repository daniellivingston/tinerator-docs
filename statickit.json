{
  "functions": {
    "sendSupportEmail": {
      "type": "sendNotification",
      "to": "support@statickit.com",
      "rules": [
        { "type": "required", "arg": "fields.message" }
      ]
    },
    "requestPayments": {
      "type": "sendNotification",
      "to": "support@statickit.com",
      "rules": [
        { "type": "required", "arg": "fields.useCase" }
      ]
    },
    "signUpSurvey": {
      "type": "sendNotification",
      "to": "support@statickit.com",
      "rules": [
        { "type": "required", "arg": "fields.useCase" }
      ]
    }
  }
}
